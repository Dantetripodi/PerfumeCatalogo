import { PerfumeInput } from "../types";

// The four minis that used to live here were the same 50ml bottles the supplier
// sells as its Jacques Ryon line (Bad Boy, Saulvag, Invic, Heroes Men), so they
// were duplicates once that line was imported. The collection stays declared so
// a genuine mini can be added from the admin panel without a schema change.
export const perfumesMinis: PerfumeInput[] = [];
